# WebP Safe Migrator - Git Ignore

# Configuration files with credentials (keep template, ignore customized versions)
webp-migrator.config.local.yaml
.env
.env.local

# Log files
*.log
logs/

# WordPress uploads and data (if running locally)
wp-content/uploads/
wp-content/webp-migrator-backup/
wp-content/webp-migrator-logs/

# Container volumes and data
data/
volumes/
mysql-data/
wordpress-data/

# Backup files
*.bak
*.backup
backup/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Windows specific
desktop.ini

# Temporary files
tmp/
temp/
*.tmp

# Node modules (if using any Node.js tools)
node_modules/

# Composer vendor (if using Composer)
vendor/

# Package files
*.zip
*.tar.gz
*.rar

# SSL certificates (keep templates, ignore real certificates)
*.pem
*.key
*.crt
*.csr
ssl-certs/
!ssl-certs/.gitkeep

# Environment specific overrides
docker-compose.override.yml
docker-compose.local.yml